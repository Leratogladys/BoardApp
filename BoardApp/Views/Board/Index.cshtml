@model IEnumerable<BoardApp.Models.Board>

@{
    ViewData["Title"] = "List of Boards";
}

<h1>List of Boards</h1>

<p>
    <a asp-action="Create">Add new board</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.First().BoardCode)</th>
            <th>@Html.DisplayNameFor(model => model.First().Make)</th>
            <th>@Html.DisplayNameFor(model => model.First().Model)</th>
            <th>@Html.DisplayNameFor(model => model.First().FlashKb)</th>
            <th>@Html.DisplayNameFor(model => model.First().Price)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.BoardCode)</td>
                <td>@Html.DisplayFor(modelItem => item.Make)</td>
                <td>@Html.DisplayFor(modelItem => item.Model)</td>
                <td>@Html.DisplayFor(modelItem => item.FlashKb)</td>
                <td>@Html.DisplayFor(modelItem => item.Price)</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.BoardCode }) |
                    @Html.ActionLink("Details", "Details", new { id = item.BoardCode }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.BoardCode })
                </td>
            </tr>
        }
    </tbody>
</table>